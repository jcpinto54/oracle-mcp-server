# Optional mirror of runtime dependencies for older workflows.
# Source of truth: dependency list in pyproject.toml (use pip install -e . or uv).
# MCP Server Dependencies
mcp>=1.0.0

# Oracle Database Driver
oracledb>=2.0.0

# Optional: For enhanced logging and formatting
rich>=13.0.0
